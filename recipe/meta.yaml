{% set name = "seqlog" %}
{% set version = "0.3.29" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 1d753827c481b78f17e5edc59fe14dd6061cfc8c30c37bea4558800518644899

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.6
  run:
    - python >=3.6
    - python-dateutil >=2.5.3
    - pyyaml >=3.11
    - requests >=2.10.0

test:
  imports:
    - seqlog
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/tintoy/seqlog
  summary: SeqLog enables logging from Python to Seq.
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - dhirschfeld
